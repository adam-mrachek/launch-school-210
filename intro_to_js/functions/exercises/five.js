// What does the following code log to the console?

function scream(words) {
  return words + '!!!';
}

scream('Yipeee!');

// It logs nothing to the console because there is no log expression. It only returns a value.